 <nav>
    <div class="nav-wrapper black">
      <a href="#" class="brand-logo black">Breaking News</a>
      <ul class = "right">

        <li>
            <a href = "/scrape" class=" white btn black-text"><i class="material-icons right">system_update_alt</i>Scrape!</a>
        </li>
              
      </ul>
    </div>
  </nav>

  <br>

  <!-- Articles -->
<div class="container">

    <table>
        <tbody>

            {{#each articles}}
            <div class=row>

                <div class="col s8">
                    <a href="{{this.link}}" class="collection-item black-text" target="_blank">{{this.title}}</a>
                </div>
                <div class="col s4">
                    <button class="noteButton waves-effect waves-light btn-flat dark blue" id="{{this._id}}">Add Note</button>
                </div>

            </div>
            {{/each}}

        </tbody>
    </table>

</div>
        
<!-- Note modal -->
<div id="noteModal" class="modal modal-fixed-footer">

    <div class="modal-content">
        <p>Notes</p>
        <ul class="messages collection"></ul>
    </div>
    
    <div class="modal-footer">
        <form name="noteForm" id="noteForm">
                    <input required placeholder="Enter a note here" type="text" name="noteText" id="noteText">
            <button class="btn green left" type="submit" id="noteButton">
                Post Note!
            </button>
            <a href="/" class="modal-action modal-close waves-effect waves-light btn-flat">Close</a>
        </form>
    </div>

</div>